import { quicksand } from '@/utils/fonts';

import './styles.css';

const OxygenHoverEffect = () => {
	return (
		<article className={`oxygenHoverWrapper ${quicksand.className}`}>
			<header>Oxygen Hover Effect</header>
			<section>
				<h2>
					<span>d</span>
					<span>o</span>
					<span>nate</span>
					<br />
					<i></i>
				</h2>
			</section>
		</article>
	);
};

export default OxygenHoverEffect;
